# 稳健性检验报告（base）

> 由 `src/verify/robustness_checks.py` 自动生成。
> 与 `TRAIN_REPORT` 同数据、同管线、同种子，指标可直接对比。


## 表 A 捷径单独成模 —— 消融 Δ 为什么不能读成「无贡献」

七组消融里，剔除任一类捷径的 Δ 都只有 0.0001~0.0002。但让每条捷径**单独**成模：

| 对照 | 名义维数 | 入模维数 | macro-F1 | agent 召回 |
|---|---|---|---|---|
| 仅 3 维协议形态元特征 | 3 | 2 | **0.7753** | 0.9999 |
| 仅 6 维窗口依赖特征 | 6 | 5 | **0.9099** | 0.9408 |
| 仅 16 维缺失模式特征（gas/nonce/value） | 16 | 14 | **0.9891** | 0.9998 |
| 仅 f_gas_avail_ratio 单个特征 | 1 | 1 | **0.5736** | 0.9999 |
| 仅 f_longest_silence_h 单个特征 | 1 | 1 | **0.7302** | 0.7787 |

**读法**：缺失模式一族单独成模即达 macro-F1 0.9891、agent 召回 0.9998；单个 `f_gas_avail_ratio` 的 agent 召回也有 0.9999。
三条捷径各自都能独立锁死 agent ⇒ 拿掉一条，剩下的立刻补位，Δ 自然≈0。
**所以 Δ≈0 证明的是「捷径之间高度冗余」，而不是「捷径没有贡献」。**
主结论请引用 behavior 集的**绝对值**，不要引用 Δ。


## 表 B Δt 截断对照 —— 采集不对称的定量影响

bot 组按 36 段 × 10,000 块采集，段内 Δt 物理上限约 **20,000 秒**；agent/human 全窗口连续，上限是整个窗口。
把三组 Δt 一律 winsorize 到该上限（不丢地址，样本量不变）后重训 behavior 集：

| 口径 | 样本 | macro-F1 | agent 召回 | bot 召回 | human 召回 | 二分类 agent→human |
|---|---|---|---|---|---|---|
| 基线（论文口径） | 30,000 | **0.9812** | 0.988 | 0.983 | 0.970 | 59.8% |
| Δt winsorize 至 20,000s | 30,000 | **0.9705** | 0.982 | 0.980 | 0.943 | 54.2% |

**读法**：主结论在截断后仍达 **0.9705**（较基线 −0.0107），远超 0.7 判定线 ⇒ 「行为指纹存在」这一核心论点不依赖采集不对称。
但二分类漏检率从 59.8% 降到 54.2%（−5.6pp）⇒ **表 5.5 那个具体区间对采集口径敏感，论文不应把它当成硬边界**，应表述为「多数 agent 被归并为 human」这一方向性结论。


## 表 C 无延迟族对照 —— 七组消融并不互相独立

model / latency / nometa / nowin / clean / behavior / timing **七组全部包含完整的 18 维延迟族**，消融只动延迟族以外的部分。
所以「七组无一例外」不是七个独立证据。真正的对照是把延迟族整族拿掉：

| 对照 | 名义维数 | 入模维数 | macro-F1 | agent 召回 | 二分类 agent→human |
|---|---|---|---|---|---|
| behavior − 延迟族（节律 6 + 交互 8） | 14 | 10 | **0.8824** | 0.965 | 48.4% |

**读法（结论比原论文更保守，必须如实写）**：拿掉延迟族后，agent 被判为 human 的比例降到 **48.4%**，已不再占多数（接近五五开）。
对比七组共享延迟族时的 56.2%~67.6%，可以确定：
- ✅ **站得住的部分**：无论哪种特征口径，agent 都被强行拆到human/bot 两侧，没有一侧能正确容纳它 —— 「二分标签空间里没有 agent 的位置」这一结构性论断成立；
- ✅ 被判为 human 的比例始终**远高于零**，且与 Web 域报道的 34.5%~39.1% 处于同一量级甚至更高；
- ⚠️ **站不住的部分**：「漏检为 human 的比例一致落在 56.2%~67.6%」与「失效方向与特征可得性无关」。这个多数方向主要由延迟族驱动 —— 整族拿掉后就降到 48.4%，再叠加表 B 的采集口径敏感性，说明该区间不是硬边界。

⇒ 论文应把结论收敛为「**agent 在二分框架下必然被错误归并，且相当大比例被归并到风控上最危险的 human 一侧**」，并同时报告本表与表 B 作为边界条件，而不是把七个共享内核的集合当成七个独立证据。


---

## 建议的论文写法

1. 5.6 节删掉「Δ=+0.0002 说明协议形态捷径贡献近乎为零」，改为「三类捷径互为冗余，逐项消融在 0.99 以上的天花板区间没有分辨率（表 A）；判别力的证据是 behavior 集的绝对值」。
2. 5.7 节删掉「七组无一例外 ⇒ 失效与特征可得性无关」这一推论（七组共享同一个延迟族内核，不构成七个独立证据），改为「agent 在二分框架下必然被错误归并，且相当大比例落到 human 一侧」，并把表 B、表 C 作为边界条件一并报告。
3. 摘要、1.4 节、5.9 节、6.1 节里所有「56.2%~67.6%」「与特征口径无关」的表述同步改口径。
4. 5.8 节局限性第 (3) 条补上表 B 的定量结果，不要只写「可能轻微影响长周期节律特征」。